# Project comparison — prior art for AgentUnison (2026-09-04)

Detailed per-project reports (with file:line evidence) live beside this file:
`fleetsmith.md`, `sync-tools.md`, `agent-collections.md`,
`harness-docs-claude-codex-opencode.md`, `harness-docs-copilot-cursor-gemini.md`.
Revisions are in `SOURCE-LINKS.md`. This file is the synthesis.

## Comparison table

| Project | Core idea | Strengths | Weaknesses | Architecture | Source of truth | Migration behavior | Symlink strategy | Validation strategy | Ideas to adopt | Ideas rejected | Why |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **FleetSmith** (Node, MIT core / AGPL `ee/`, v0.7.1) | Compile one `fleet.yaml` (agents, capabilities, handoff graph) into Claude Code / opencode / goose harnesses | Pure adapters → `FileSet` → single writer; capability→permission maps with per-harness quirk notes; documented degradation table; deterministic QA battery + drift-by-recompile in CI; opt-in live `--exec` runs that never gate; SEA binary + `npx github:` + smoke-tested artefact; one runtime dep | **No repository analysis at all**; whole-file `CLAUDE.md`/`AGENTS.md` pointers clobber human content; refuse-or-`--force` only (no merge, no backup); no manifest, no uninstall, no orphan detection; POSIX-only hooks; fleet/handover model is orchestration, not convergence | spec → compile → adapters → FileSet → write | `fleet.yaml` | None for existing harnesses (build refuses on differing files) | None (dev-only junction) | Lint + QA on compiled output; lexical trigger eval; live exec advisory | FileSet/single-writer; capability KB + degradation table; QA-on-output + CI drift gate; live-exec posture; packaging playbook; `patch` with comment-preserving YAML edits | Fleet/pattern/handover model; whole-file pointers; no-manifest; hand-rolled argv parser; SubagentStop handoff content | Orchestration ≠ convergence; adopting existing repos needs merge/ownership, which it lacks |
| **agent-smith** (Python, Apache-2.0) | Orientation (`AGENTS.md`) / durable knowledge (OKF wiki) / actions (`.agents/skills`) / harness adapters as projections | Best taxonomy; loud alias fallback ladder (symlink → pointer file; dir: symlink → junction → fail); refuses to replace a real dir even with `--force`; marker-managed block in `AGENTS.md` with self-heal; `.git/info/exclude` tracking tiers; deterministic validators + CI template; stage → review → transactional promote | OKF/OpenWiki: LLM-in-the-loop knowledge compiler with heavy toolchain (Node/pnpm/fnm/OAuth); LLM-authored adapters "by reasoning from docs"; all-rights-reserved generated content | canonical files + projections + validators | `AGENTS.md`, `okf/`, `.agents/skills` | Aliases into harness paths; no reverse adoption of native files | Relative symlink first, explicit fallbacks, never silent | Zero-LLM validators (slug dupes, unclosed fences), CI | Taxonomy; alias ladder; managed block; tracking tiers; stage/promote for generated files; runtime "installed ≠ active" detection | OKF/OpenWiki pipeline; LLM adapters; license terms | Different problem (repo memory), non-deterministic, heavy |
| **agent-sync** (TS) | Sync skills/instructions/MCP across harnesses | Cycle detection + inode identity checks; neutral MCP model → per-harness renderers | Absolute two-hop symlink chains (documented data-loss incident); N identical instruction copies, no canonical; silent symlink→junction→copy fallback that still reports "created"; regex TOML patching; `$HOME` backups | copier/linker | none (its own repo gitignores the source) | none | absolute, chained | structural tests only | Cycle/inode checks; neutral-model→renderer shape | Everything else listed | Its postmortem is the argument for manifest + plan + relative links |
| **agentsync** (Bash + SKILL.md) | Copy from `agents/` into four native formats, LLM prose as engine | **Only per-directory ownership manifest** (`.agentsync-manifest`); version stamp + upgrade ledger; co-ownership detection; audit→report→approve→apply; delimited `.gitignore` block; genuine generated adapter (Codex `[agents.<name>]` registration, found by real testing) | Agents authored 4× natively; copies not links; `cp -f settings.json`; truncate-and-rebuild `config.toml`; no dry-run; prose orchestration drops steps | manifest + copy fan-out | `agents/` (4 formats) | approve→apply | none (copies) | none automated | Ownership manifest; upgrade ledger; co-ownership warning; Codex registration adapter; foreign/orphan reporting | 4 hand-authored sources; clobbering config writes; prose engine | "Four sources is zero sources" |
| **hana** (Rust) | Single-copy skills: collect into canonical, symlink back, broadcast | Cleanest collection model (move real dir → canonical → link back); relative symlinks asserted in tests; "same resolved path ⇒ native, no projection" state; good status vocabulary; dry-run | `--force` = `rm -rf`; exit code always 0; "filesystem is the state" (no manifest, so missing = drift or intent?); deletes any broken symlink it finds; Unix-only | linker with status | canonical skills dir | reverse collection of real dirs | relative symlinks | structural | Collection algorithm; native-path detection; status vocabulary; relative links | Force-delete; exit 0; no manifest; deleting unowned links | Data loss by flag; CI-useless exit codes |
| **sync-skills** (Bash) | Pointer indirection to a single skill source; reversible release | `.sync-origin` origin marker → reversible per item; dry-run default + `--confirm`; inode verification; land-before-delete ordering; best frontmatter portability analysis (signal ≠ verdict, broken vs degraded, Codex `openai.yaml` sidecar, context cost) | User-global only; bash; no tests; asks on every scan to resolve ambiguity | linker | `~/.skill-source` | release re-attributes by origin | pointer + symlink | manual | Origin marker; land-before-delete; portability rule engine; context-cost report | `$HOME` scope; ask-every-scan | Repo-scoped tool must record intent once |
| **wshobson/agents** (Markdown + Python, MIT; 202 agents/183 skills) | Marketplace of plugins projected one-way into `.codex/.opencode/.copilot/...` | Code-level 17-field capability matrix; tri-state `tools:`→permission mapping; model alias resolution with warnings; fence-aware body splitting at Codex 8 KB cap; output-root jail; installer refuses real files; CI "regenerate → `git status` clean"; real-CLI smoke (`opencode agent list`, `codex doctor`) | No ingest, no dry-run, no state; no generated-file banners; mtime staleness; hooks/MCP silently dropped; hand-rolled YAML ×4; **≈70% generic personas**, 30 duplicated basenames | marketplace → adapters → projections | `plugins/<name>/` | none | none (copies) | CI regenerate-clean + CLI smoke | Capability matrix as code+data; tri-state tools; projection validators; regenerate-clean gate; CLI smoke tests; duplicate-basename hash check | Marketplace data model; silent feature drop; template agents | Proves the "fewer, domain-shaped" thesis by counterexample |
| **cc-agents** | byte-identical fork of wshobson (0 ahead / 0 behind) | — | — | — | — | — | — | — | nothing new | — | Not a separate data point |
| **agents-skills-sync** (Deno/TS, MIT, v0.5.1) | Push central content into projects via symlinks on a cron tick | Five-way target classifier (`missing/own-link/foreign-link/real-file/real-dir`); **quarantine-never-delete** with TTL; strict managed `.gitignore` block + repair; per-file SHA-256 drift map; `--check` exit taxonomy; dry-run vocabulary; declarative multi-rooted tool profiles; escaping-symlink rejection; hermetic integration tests | Remote/cron/PAT model; absolute links into `~/.config` cache; read-only chmod; content-blind; gitignore-everything; macOS/Linux only | engine + profiles + registry | remote content repo | quarantine on collision | absolute into staging | structural + exit codes | Classifier; quarantine; managed block; hash map; exit taxonomy; profile shape | Distribution model; absolute links; content-blind | Our truth lives in the repo and is committed |
| **Adobe harness guide** (docs + prompt skills, Apache-2.0, v1.1.0 2026-08-05) | Prescriptive harness: `AGENTS.md` + `INVARIANTS.md` + `.agents/skills`; `CLAUDE.md` 3-line `@` shim; `.claude/skills/<n>` relative symlinks | Excellent audit checklists (10-question health check, 8-section audit, before/after probe); "pointers, never content"; line/token budgets; `Enforced by:` annotations; honest Windows caveat | Delete-only stance on Cursor/Copilot native files; symlink-only; LLM-prompt-executed checks; no dry-run/manifest; 16 tool claims unverified by the guide itself | prose + templates | `AGENTS.md`, `INVARIANTS.md`, `.agents/skills` | migrate then delete originals | relative symlinks, no fallback | prompt-based | Audit checklists → deterministic lints; shim form; budgets; enforced-by annotations; before/after probe as optional live check | Deleting native files; symlink-only; prompt-only validation | Native-only content has legitimate value; deletion must be a choice |

## Synthesis — what nobody does, and what AgentUnison must do

1. **Inspect → understand → converge → project → verify → protect in one deterministic tool.** Every project does one or two steps: FleetSmith compiles, linkers link, agentsync copies, Adobe prescribes. None inventories an existing repo's harness and plans a safe convergence.
2. **Manifest + symlinks together.** Linkers (hana, sync-skills, agent-sync, agents-skills-sync) have no committed ownership record; the one tool with a manifest (agentsync) copies. Ownership must be recorded so that "missing" can be told apart from "intentionally absent", uninstall can be exact, and foreign files are never touched.
3. **`CLAUDE.md = canonical + delta`.** Nobody models it. hana links unconditionally; agent-sync copies N times; Adobe/agent-smith shim only. AgentUnison must audit the delta and let the user choose adopt-into-canonical vs keep-as-harness-specific.
4. **Version-sensitive capability matrix backed by tests against real binaries.** Every tool learned harness facts by hand (Codex ignores `.claude/skills`; Codex agents need registration; junction semantics; restart-to-rescan). Two independent tools converged on the same frontmatter-portability field list. The matrix must be data (YAML) with evidence + date, and the verify step must run the real CLIs where installed.
5. **Loud fallback ladders, never silent copies.** agent-smith's ladder vs agent-sync's silent fallback is the single most instructive contrast.
6. **Quarantine, never delete by default.** agents-skills-sync's model, in-repo (`.agentunison/quarantine/<ts>/`), listed in the plan, reversible.
7. **Fewer, domain-shaped assets.** wshobson's 202 agents (≈70% generic) is the counterexample; Adobe's "skills per recurring task type only" and agentsync's ≤80-line grounded repo skill are the right size.

## Cross-cutting facts that constrain the design (from the harness research)

- `AGENTS.md` is read natively by Codex, OpenCode, Copilot (cloud agent + CLI + VS Code root), Cursor; **not** by Claude Code (import/symlink) or Gemini CLI (needs `context.fileName` or a `GEMINI.md` `@` shim).
- `.agents/skills` is read natively by Codex, OpenCode, Copilot, Cursor, Gemini; **not** by Claude Code (`.claude/skills` only; a directory symlink is followed — verified).
- Subagent formats diverge (Claude Markdown, Codex TOML + registration, OpenCode Markdown, Copilot `.agent.md`, Cursor, Gemini) — no standard; anything cross-harness here is a *generated adapter* with a documented degradation list.
- Symlink following is verified only for Claude Code / Codex / OpenCode on macOS today; undocumented for Copilot/Cursor/Gemini with Windows breakage reports on all three. Policy must be per harness × platform, with copy/generated fallbacks that are loud and manifest-recorded.
